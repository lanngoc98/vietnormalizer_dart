name: vietnormalizer_dart
description: Vietnamese text normalizer for TTS — expands numbers, dates, times, acronyms, and transliterates non-Vietnamese words.
version: 1.0.0
repository: https://github.com/nghimestudio/vietnormalizer

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
